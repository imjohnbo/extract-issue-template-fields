{
  "name": "extract-issue-template-fields",
  "version": "0.0.1",
  "description": "Extract Issue Template Fields",
  "main": "dist/index.js",
  "scripts": {
    "lint": "semistandard",
    "build": "ncc build index.js -o dist",
    "all": "npm run lint && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/imjohnbo/extract-issue-template-fields.git"
  },
  "semistandard": {
    "ignore": [
      "dist/**",
      "node_modules/**"
    ]
  },
  "author": "imjohnbo",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^4.0.0",
    "js-yaml": "^3.14.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0",
    "semistandard": "^16.0.0"
  }
}
